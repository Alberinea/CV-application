import Input from './input/Input';
import Preview from './preview/Preview';

const Main = (): JSX.Element => {
  return (
    <main>
      <Input />
      <Preview />
    </main>
  );
};

export default Main;
